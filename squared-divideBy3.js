// Squared Number
const square = (num) => {
  let squaredNumber = Math.pow(num,2);
  console.log("Squared Number: ", squaredNumber)
}
square(5)
//########################################################################

// Divide 3 Numbers
const divideThreeNums = (num1, num2, num3) => {
  let numA = num2/num1;
  let numB = num3/numA;
  console.log("Divide Three Numbers: ", numB)
}
divideThreeNums(10, 5, 2)
