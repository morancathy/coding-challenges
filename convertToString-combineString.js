// Convert to String
const convertToString = (num1, num2, num3) => {
  let addNums = num1 + num2 + num3;
  let addNumsString = addNums.toString();
  return addNumsString;
 };

console.log("Convert To String: ", convertToString(1, 6, 2));
